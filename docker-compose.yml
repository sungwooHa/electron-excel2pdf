version: '3'
services:
  excel2pdf:
    build: .
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - .:/app
      - /dev/shm:/dev/shm
    network_mode: host
    privileged: true 